[build]
  publish = "."
  command = ""
  
[build.environment]
  NODE_VERSION = "18"

# Functions 폴더 무시
[functions]
  directory = "none"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
